# Runtime dependencies.
# Typed configuration (TASK-003): pydantic-settings enforces the APP_ variable
# conventions described in docs/configuration.md; python-dotenv (also a
# pydantic-settings dependency) is used directly to inspect .env for
# unknown APP_ keys.
pydantic>=2.12
pydantic-settings>=2.10
python-dotenv>=1.0

# Kafka transport client (TASK-008).
confluent-kafka>=2.15,<3

# Polars for analytical processing (TASK-013).
polars>=1.0,<2

# S3-compatible object-storage client for MinIO / AWS S3 (TASK-020).
boto3>=1.35,<2

# Apache Arrow for Parquet I/O (TASK-021).
pyarrow>=14.0,<20

# HTTP client for source adapters (TASK-035).
httpx>=0.27,<1

# HTML parsing for web retailer adapters (TASK-047).
selectolax>=0.3,<1

# Retry with exponential backoff for source adapters (TASK-041).
tenacity>=8.0.0

# FastAPI web framework for the API service (TASK-063).
fastapi>=0.115,<1
uvicorn>=0.32,<1
sqlalchemy>=2.0

# PostgreSQL adapter for warehouse-loader and health persistence (TASK-059, TASK-075).
psycopg2-binary>=2.9

# Prometheus metrics export (TASK-084).
prometheus-client>=0.21,<1

# OpenTelemetry SDK for distributed tracing (TASK-090).
opentelemetry-api>=1.25,<2
opentelemetry-sdk>=1.25,<2
opentelemetry-exporter-otlp-proto-grpc>=1.25,<2

# LangGraph agent routing framework (TASK-098).
langgraph>=1.0,<2
