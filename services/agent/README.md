# agent/

Agent service (LangGraph). Provides controlled analytical/operational assistance using explicit read-only tools over platform state. It must not mutate platform state or execute arbitrary SQL writes. See `ai/SPECIFICATION.md` §6.6.
