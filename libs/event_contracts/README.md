# event_contracts/

Shared event contract models: the canonical event envelope and payload definitions used by Kafka producers and consumers. See `ai/SPECIFICATION.md` §7 (Event Contract).

The Python package is named `event_contracts` (snake_case) because Python import paths cannot contain hyphens. The original boundary name in the specification is `event-contracts/`.
